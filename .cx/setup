#!/usr/bin/env bash
# Prepares a fresh worktree. cx runs this after creating it.
set -euo pipefail
export PATH="$HOME/.local/bin:$PATH"
uv sync -q
if [ -f web/package.json ]; then (cd web && npm ci --no-audit --no-fund); fi
