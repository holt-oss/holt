# Branches to include in the staging preview that have no PR labelled
# `staging` yet. One per line; blank lines and # comments are ignored.
# Looked up on origin first, then in the local clone at $HOLT_LOCAL_REPO.
# A branch whose PR later gets the label is included only once.
worktree-holt-web-app-frontend
